# Product modules

This directory is intentionally empty of product behavior.

When the product brief exists, add domain modules here. A module should own its domain types, application logic, data access boundary, and tests. Product modules may depend on `src/platform`; `src/platform` must never depend on product modules.
